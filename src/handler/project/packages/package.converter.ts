import {PackageHandler} from "@handler/project/packages/package.handler";

export class PackageConverter {
    static toJson(packageHandler: PackageHandler): string {
        let toReturn = "";

    }
}